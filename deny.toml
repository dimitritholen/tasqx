[graph]
all-features = true
targets = [
  "x86_64-unknown-linux-gnu",
  "x86_64-pc-windows-msvc",
  "aarch64-apple-darwin",
]

[advisories]
unmaintained = "workspace"
unsound = "workspace"
# Exceptions must use the structured owner/reason/expiry format documented in
# docs/dependency-policy.md. Bare advisory IDs and blanket ignores are banned.
ignore = []

[licenses]
confidence-threshold = 0.8
allow = [
  "Apache-2.0",
  "Apache-2.0 WITH LLVM-exception",
  "0BSD",
  "BSL-1.0",
  # tasqx's OWN license (the workspace crates are in this check's graph). No
  # dependency is expected to carry it; one that did would deserve a look, but
  # the gate cannot distinguish "ours" from "theirs" by license id alone.
  "FSL-1.1-MIT",
  "MIT",
  "MPL-2.0",
  "Unicode-3.0",
  "Zlib",
]
exceptions = []

[licenses.private]
ignore = true

[bans]
multiple-versions = "warn"
wildcards = "deny"
highlight = "simplest-path"
deny = []
skip = []
skip-tree = []

[sources]
unknown-registry = "deny"
unknown-git = "deny"
allow-registry = ["https://github.com/rust-lang/crates.io-index"]
allow-git = []
